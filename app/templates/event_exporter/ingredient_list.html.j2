{% extends "base/base.html.j2" %}

{% block content %}

<h2 class="text-uppercase font-comfortaa">Suroviny (pro {{ event.people_count }} lidí)</h2>
<ul>
    {% for ingredient in ingredients %}
    	<li>
    		<details>
    			<summary class="row">
        			<span class="col font-weight-bold">{{ ingredient.name }}</span>
                	<span class="col font-weight-bold">{{ ingredient.amount | int }}</span>
                	<span class="col font-weight-bold">{{ ingredient.measurement.name }}</span>
    			</summary>

		    	{% for recipe in ingredient.event_recipes %}
                    <li class="row"> 
                        <span class="col"> {{ link_to(recipe) }} {% if recipe.occurences > 1 %}({{ recipe.occurences }}x){% endif %}</span>
                        <span class="col"> {{ amounts[ingredient.id]["recipes"][recipe.id]["amount"] | int }} {% if recipe.occurences > 1 %}(x{{ recipe.occurences }}){% endif %} </span>
                        <span class="col"> {{ ingredient.measurement.name }} </span>
                    </li>
		    	{% endfor %}
    		</details>
    	</li>
    {% endfor %}
</ul>

{% endblock %}
