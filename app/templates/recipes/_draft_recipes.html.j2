{% if current_user.draft_recipes %}

<div id="draft-recipes" class="mt-3 col-12">
    <div class="container p-3 bg-color-secondary dashboard-badge">

        <h6 class="font-comfortaa text-uppercase"> Dokončit rozdělané recepty </h6>

        <ul class="no-list">
        {% for recipe in current_user.draft_recipes %}
            <li> {{ link_to_edit(recipe) }} </li>
        {% endfor %}
        </ul>    

    </div>
</div>

{% endif %}
