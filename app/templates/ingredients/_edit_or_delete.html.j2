<div>
    <a class="btn bg-color-secondary-action color-white me-3 d-inline-flex" href="{{ url_for('IngredientsView:edit', id=ingredient.id) }}">{{ texts.edit }} {{ icon("edit", class="ps-1") }}</a>
    <form class="d-inline form" action="{{ url_for('IngredientsView:delete', id=ingredient.id) }}" onsubmit="return confirm('{{ texts.ingredient_delete_confirm }}');" method="post" accept-charset="utf-8">
        {% if ingredient.is_used == False %}
            <button type="submit" class="btn bg-color-secondary-action color-white">{{ texts.ingredient_delete }} {{ icon("delete") }}</button>
        {% else %}
            <button type="submit" class="btn bg-color-secondary-action color-white" disabled>{{ texts.delete_error }} {{ icon("delete") }}</button>
        {% endif %}
    </form>
</div>