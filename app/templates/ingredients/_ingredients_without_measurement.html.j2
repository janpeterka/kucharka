{% if current_user.ingredients_without_measurement %}

<div id="ingredients-without-measurement" class="mt-3 col-12">
    <div class="container bg-color-secondary p-3 dashboard-badge">

        <h6 class="font-comfortaa text-uppercase"> přidat míru surovinám </h6>

        <ul class="no-list">
        {% for ingredient in current_user.ingredients_without_measurement %}
            <li>{{ link_to_edit(ingredient) }}</li>
        {% endfor %}
        </ul>

    </div>
</div>

{% endif %}
