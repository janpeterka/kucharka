<turbo-frame id="measurement-{{ measurement.id }}">
  <li class="editing">
    <div>
      <label></label>
    </div>
    <form method="PUT" action="{{ url_for('MeasurementsView:put', id=measurement.id) }}">
      <input type="text" name="measurement" value="{{ measurement.name }}" class="edit" autofocus>
    </form>
  </li>
</turbo-frame>