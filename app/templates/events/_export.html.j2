<details class="colored mb-2">
<summary> Exporty </summary>
<div>
	<div class="mb-2">
		<a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_shopping_list', event_id=event.id) }}"> {{ icon("shopping") }} nákupní seznam </a>
		<a data-turbo="false" class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_ingredient_list', event_id=event.id) }}"> {{ icon("list") }} nákupní seznam (s recepty) </a>
		<a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_recipe_list', event_id=event.id) }}"> {{ icon("food") }} jídelníček </a>
		<a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_recipe_list_visual', event_id=event.id) }}"> {{ icon("food") }} jídelníček (kanban) </a>
		<a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_cookbook', event_id=event.id) }}"> {{ icon("cookbook") }} kuchařka </a>
	</div>
	<div class="mb-2">
		<a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_shopping_list_pdf', event_id=event.id) }}" target="_blank"> {{ icon("pdf") }} nákupní seznam </a>
		<a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_ingredient_list_pdf', event_id=event.id) }}" target="_blank"> {{ icon("pdf") }} nákupní seznam (s recepty)  </a>
		<a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_recipe_list_pdf', event_id=event.id) }}" target="_blank"> {{ icon("pdf") }} jídelníček  </a>
		{# <a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_recipe_list_visual_pdf', event_id=event.id) }}"> {{ icon("food") }} jídelníček (kanban) {{ icon("pdf") }} </a> #}
		<a class="btn btn-secondary me-2 mt-1 mt-lg-0" href="{{ url_for('EventExporterView:show_cookbook_pdf', event_id=event.id) }}" target="_blank"> {{ icon("pdf") }} kuchařka </a>
	</div>
</div>
</details>