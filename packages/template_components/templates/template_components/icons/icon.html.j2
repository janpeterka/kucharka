{% if tooltip %}
    <abbr title="{{ tooltip }}" style="text-decoration: none">
{% endif %}

<i {{ render_class(class) }} {{ render_data(data) }}
   {% if style %}style="{{ style }}"{% endif %}>
   {{ value if value }}
</i>

{% if tooltip %}
    </abbr>
{% endif %}
